export interface IFeatureCard {
  imageSrc: string;
  title: string;
  description: string;
  linkText: string;
  linkHref: string;
}

export interface IDiscoverCard {
  imageSrc: string;
  title: string;
  description: string;
}
